package constant

const (
	RedisOperationFailedError   = "Redis Operation Failed"
	RequiredFieldError          = "%s fied is required"
	PasswordFomatContraintError = "Password must contain at least one uppercase letter, one lowercase letter, one number, and one special character"
)
